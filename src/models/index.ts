export interface IUser {
  email: string;
  firstName: string;
  lastName: string;
  studentNo: number;
}

export interface Job {
  id: number;
  title: string;
  company: string;
  location: string;
  description: string;
  contactPerson: string;
  contactMail: string;
  jobDate: string;
}
